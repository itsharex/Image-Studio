name: Release

# Build desktop targets in parallel and produce Android APKs for phone and pad
# shells from the bundled WebView wrapper in `android-shell/`.

on:
  push:
    tags:
      - "v*"
  workflow_dispatch:

permissions:
  contents: write

jobs:
  prepare-version:
    name: Prepare version metadata
    runs-on: ubuntu-24.04
    outputs:
      product_version: ${{ steps.version.outputs.product_version }}
      app_version: ${{ steps.version.outputs.app_version }}
      frontend_version: ${{ steps.version.outputs.frontend_version }}
      android_version_name: ${{ steps.version.outputs.android_version_name }}
      android_version_code: ${{ steps.version.outputs.android_version_code }}
      version_channel: ${{ steps.version.outputs.version_channel }}
      version_sha: ${{ steps.version.outputs.version_sha }}
    steps:
      - name: Checkout
        uses: actions/checkout@v4
        with:
          fetch-depth: 0

      - name: Compute version
        id: version
        shell: bash
        run: |
          eval "$(./scripts/compute-version.sh)"
          {
            echo "product_version=${PRODUCT_VERSION}"
            echo "app_version=${APP_VERSION}"
            echo "frontend_version=${FRONTEND_VERSION}"
            echo "android_version_name=${ANDROID_VERSION_NAME}"
            echo "android_version_code=${ANDROID_VERSION_CODE}"
            echo "version_channel=${VERSION_CHANNEL}"
            echo "version_sha=${VERSION_SHA}"
          } >> "$GITHUB_OUTPUT"

  build-desktop:
    name: Build ${{ matrix.id }}
    runs-on: ${{ matrix.runner }}
    needs: prepare-version
    strategy:
      fail-fast: false
      matrix:
        include:
          - id: windows-amd64
            runner: windows-latest
            goos: windows
            artifact_name: image-studio-windows-amd64.exe
            artifact_path: dist/image-studio-windows-amd64.exe
            wails_args: -platform windows/amd64 -clean
            package_cmd: cp image-studio/build/bin/image-studio.exe dist/image-studio-windows-amd64.exe
          - id: macos-universal
            runner: macos-latest
            goos: darwin
            artifact_name: image-studio-macos-universal.zip
            artifact_path: dist/image-studio-macos-universal.zip
            wails_args: ""
            package_cmd: ditto -c -k --sequesterRsrc --keepParent "image-studio/build/bin/Image Studio.app" dist/image-studio-macos-universal.zip
          - id: linux-amd64
            runner: ubuntu-24.04
            goos: linux
            artifact_name: image-studio-linux-amd64.tar.gz
            artifact_path: dist/image-studio-linux-amd64.tar.gz
            wails_args: -clean -tags webkit2_41
            package_cmd: tar -C image-studio/build/bin -czf dist/image-studio-linux-amd64.tar.gz image-studio

    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Set up Go
        uses: actions/setup-go@v5
        with:
          go-version: "1.25"
          check-latest: true

      - name: Set up Node
        uses: actions/setup-node@v4
        with:
          node-version: "20"
          cache: npm
          cache-dependency-path: image-studio/frontend/package-lock.json

      - name: Sync version metadata
        shell: bash
        env:
          IMAGE_STUDIO_PRODUCT_VERSION: ${{ needs.prepare-version.outputs.product_version }}
          IMAGE_STUDIO_FRONTEND_VERSION: ${{ needs.prepare-version.outputs.frontend_version }}
        run: node ./scripts/sync-version-metadata.mjs

      - name: Install Linux build dependencies
        if: matrix.goos == 'linux'
        run: |
          sudo apt-get update
          sudo apt-get install -y libgtk-3-dev libwebkit2gtk-4.1-dev

      - name: Install Wails CLI
        if: matrix.goos != 'darwin'
        shell: bash
        run: |
          go install github.com/wailsapp/wails/v2/cmd/wails@latest
          echo "$(go env GOPATH)/bin" >> "$GITHUB_PATH"

      - name: Build desktop app
        working-directory: image-studio
        shell: bash
        env:
          VITE_APP_VERSION: ${{ needs.prepare-version.outputs.app_version }}
        run: |
          if [ "${{ matrix.goos }}" = "darwin" ]; then
            bash ../scripts/package-local-macos-app.sh
          else
            wails build ${{ matrix.wails_args }} \
              -ldflags "-X github.com/yuanhua/image-gptcodex/pkg/client.Version=${{ needs.prepare-version.outputs.app_version }}"
          fi

      - name: Stage packaged artifact
        shell: bash
        run: |
          mkdir -p dist
          ${{ matrix.package_cmd }}
          ls -la dist/

      - name: Upload build artifact
        uses: actions/upload-artifact@v4
        with:
          name: ${{ matrix.artifact_name }}
          path: ${{ matrix.artifact_path }}
          retention-days: 14

  build-android-apk:
    name: Build ${{ matrix.id }} apk
    runs-on: ubuntu-24.04
    needs: prepare-version
    strategy:
      fail-fast: false
      matrix:
        include:
          - id: phone
            gradle_task: assemblePhoneRelease
            artifact_name: image-studio-android-phone-release.apk
            artifact_path: android-shell/app/build/outputs/apk/phone/release/app-phone-release.apk
          - id: pad
            gradle_task: assemblePadRelease
            artifact_name: image-studio-android-pad-release.apk
            artifact_path: android-shell/app/build/outputs/apk/pad/release/app-pad-release.apk
    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Set up JDK
        uses: actions/setup-java@v4
        with:
          distribution: temurin
          java-version: "17"

      - name: Set up Node
        uses: actions/setup-node@v4
        with:
          node-version: "20"
          cache: npm
          cache-dependency-path: image-studio/frontend/package-lock.json

      - name: Sync version metadata
        shell: bash
        env:
          IMAGE_STUDIO_PRODUCT_VERSION: ${{ needs.prepare-version.outputs.product_version }}
          IMAGE_STUDIO_FRONTEND_VERSION: ${{ needs.prepare-version.outputs.frontend_version }}
        run: node ./scripts/sync-version-metadata.mjs

      - name: Set up Android SDK
        uses: android-actions/setup-android@v3

      - name: Install Android packages
        run: sdkmanager "platform-tools" "platforms;android-34" "build-tools;34.0.0"

      - name: Prepare Android user home
        run: mkdir -p .tmp/android-home/.android

      - name: Write Android local.properties
        run: |
          printf 'sdk.dir=%s\n' "$ANDROID_SDK_ROOT" > android-shell/local.properties

      - name: Set up Gradle
        uses: gradle/actions/setup-gradle@v4
        with:
          gradle-version: "8.7"

      - name: Build ${{ matrix.id }} apk
        env:
          ANDROID_USER_HOME: ${{ github.workspace }}/.tmp/android-home/.android
          ANDROID_SDK_HOME: ${{ github.workspace }}/.tmp/android-home
          HOME: ${{ github.workspace }}/.tmp/android-home
          IMAGE_STUDIO_ANDROID_VERSION_NAME: ${{ needs.prepare-version.outputs.android_version_name }}
          IMAGE_STUDIO_ANDROID_VERSION_CODE: ${{ needs.prepare-version.outputs.android_version_code }}
          VITE_APP_VERSION: ${{ needs.prepare-version.outputs.app_version }}
        run: |
          gradle -p android-shell --no-daemon ${{ matrix.gradle_task }}

      - name: Verify signed apk
        env:
          ANDROID_USER_HOME: ${{ github.workspace }}/.tmp/android-home/.android
          ANDROID_SDK_HOME: ${{ github.workspace }}/.tmp/android-home
          HOME: ${{ github.workspace }}/.tmp/android-home
        run: |
          "$ANDROID_SDK_ROOT/build-tools/34.0.0/apksigner" verify -v "${{ matrix.artifact_path }}"

      - name: Upload apk artifact
        uses: actions/upload-artifact@v4
        with:
          name: ${{ matrix.artifact_name }}
          path: ${{ matrix.artifact_path }}
          retention-days: 14

  publish-release:
    name: Publish release assets
    runs-on: ubuntu-24.04
    needs:
      - prepare-version
      - build-desktop
      - build-android-apk
    if: startsWith(github.ref, 'refs/tags/v')
    steps:
      - name: Download all artifacts
        uses: actions/download-artifact@v4
        with:
          path: dist

      - name: Flatten artifact layout
        shell: bash
        run: |
          mkdir -p release-assets
          find dist -type f -maxdepth 2 -exec cp {} release-assets/ \;
          ls -la release-assets/

      - name: Attach to release
        uses: softprops/action-gh-release@v2
        with:
          name: v${{ needs.prepare-version.outputs.product_version }}
          tag_name: v${{ needs.prepare-version.outputs.product_version }}
          files: release-assets/*
          fail_on_unmatched_files: true
          generate_release_notes: false
